public with sharing class BacenApiConsumer {

    // HttpRequestWrapper wrapper = new HttpRequestWrapper();
    // public CurrencyType getCurrrencyData() {
        // wrapper.get(endpoint, data);
    // }
}